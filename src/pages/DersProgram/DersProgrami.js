import React from 'react';
import { SafeAreaView } from 'react-native';
import ScheduleScreen from './DersProgramiStyle';

const App = () => {
  return (
    <SafeAreaView style={{ flex: 1 }}>
      <ScheduleScreen />
    </SafeAreaView>
  );
};

export default App;
